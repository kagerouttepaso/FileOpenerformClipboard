<Window x:Class="FileOpenerFromClipboardGui.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileOpenerFromClipboardGui"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance d:Type=local:MainWindowViewModel,IsDesignTimeCreatable=True}"
        Title="MainWindow" Width="200" Height="130"  WindowStyle="ToolWindow" ContentRendered="Window_ContentRenderedAsync" >
    <Grid Margin="0" >
        <Grid.RowDefinitions>
            <RowDefinition Height="15"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Valid Path Searching." HorizontalAlignment="Center" Grid.Row="0" Grid.RowSpan="1"/>
        <ProgressBar Height="30" Value="{Binding Progress}" Grid.Row="1" Grid.RowSpan="1"/>
        <Button Content="Cancel" Grid.Row="2" Grid.RowSpan="1" Click="Button_Click"/>
    </Grid>
</Window>
